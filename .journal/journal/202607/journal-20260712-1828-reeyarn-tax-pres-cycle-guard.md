# Session Journal — reeyarn — 2026-07-12 18:28

- XBRL Taxonomy Engine > Linkbase parsing: Fixed the SIGSEGV in `tax_pres._traverse` with a path-scoped cycle guard (commit `6439c60`) — filers ship calculation linkbases containing directed cycles (forbidden by XBRL 2.1 §5.2.5.2 but real), and the unguarded `chain_dn` recursion blew the C stack on 18 of 10,287 EU filings; because a segfaulted `multiprocessing.Pool` worker never returns, this presented downstream as an indefinite HANG rather than an error.
- XBRL Taxonomy Engine > Linkbase parsing: Made the guard **path-scoped** rather than global, mirroring the `stack` idiom already in `BaseSet.get_branch_members` — `chain_dn` is a DAG, so a concept may legitimately sit under several parents; a global visited set would silently drop those re-visits and change output for every filing. This is also why the presentation side never crashed while the calculation side did.
- XBRL Taxonomy Engine > Linkbase parsing: Proved the linkbases are genuinely **cyclic**, not merely deep, using an iterative three-colour DFS — depth cannot fool it, so it replaces the handoff's recursion-limit inference with an actual proof; the cycles are all in the **calculation** linkbase (e.g. Telekom Austria: `OtherComprehensiveIncome -> ReclassificationAdjustments... -> OtherComprehensiveIncome`), never the presentation one.
- XBRL Taxonomy Engine > Testing: Verified 18/18 previously-crashing filings now parse with zero segfaults — including `350798/2021`, the acceptance test, whose cached pickle failed the oracle and could not be repaired by any workaround, so the cache-based recovery script is now obsolete.
- XBRL Taxonomy Engine > Testing: Built a regression gate showing 40/40 byte-identical `concept_df` (33,334 rows) between old and new `.so` — the first run of this gate "failed" 38/40, but a same-binary control proved the *instrument* was broken: `segment_axes`/`segment_members` are `list(<set>)`, so their order shifts with `PYTHONHASHSEED` and any two runs differ. Fixed the gate rather than weakening it.
- XBRL Taxonomy Engine > Audit: Established by AST scan that `_traverse` was the only live unguarded recursion that fires on this corpus — `process_table_structure` had the same shape and was guarded prophylactically; `process_children` is unguarded but dead (no callers).
- EDGAR Integration > Diagnosis: Discovered that `tax.base_sets` registers every linkbase **twice** — once as a `BaseSet` (string key, from `xlink.conn_cc`) and once as an `XLink` (tuple key, from `taxonomy.compile_*_networks`) — so `tax_pres` walks each network twice and emits every concept row twice (rows 170→340 while unique concepts stay 95→95).
- EDGAR Integration > Diagnosis: Found that this duplication corrupts a downstream research variable: MDIS's `mdis_1101_read_xbrl.py:739` sets `num_concepts_sop = len(sop_df)` — a raw row count with no dedup — which feeds the means/quantiles of Tables 3/4. proj_esef is immune because `tag_concepts()` deduplicates (its primary/role_code counts were provably identical with and without the fix).
- EDGAR Integration > Diagnosis: Attempted a `base_sets` union-dedup and **reverted it** — it validated cleanly on ESEF (8/8 filings: rows halve, unique concepts and statement counts preserved) but loses **210 unique concepts and 8 whole statements** on EDGAR (AAPL 2020, 891→681 unique), proving the two representations have asymmetric, filing-family-dependent coverage and neither is a superset. Documented so it is not naively retried.
- XBRL Taxonomy Engine > Version control: Committed the pre-existing uncommitted BaseSet support separately (`99b2ad7`), flagged in the message as NOT session-authored — `git log -S_traverse` shows it never appeared in any commit, and it is the sole cause of the SIGSEGV (HEAD does not crash on those 18 filings at all), so recording it first gives the fix a clean, reviewable baseline.
- XBRL Taxonomy Engine > Version control: Recovered `openesef/taxonomy/taxonomy.py` after clobbering its uncommitted changes with `git checkout --` — reconstructed the exact content from a dangling stash blob by reversing my five edit-sites, and verified the restore reproduces pre-session behaviour field-for-field (`pres_str=10, calc_str=4`, 192 rows) and the full 18/18 gate.
