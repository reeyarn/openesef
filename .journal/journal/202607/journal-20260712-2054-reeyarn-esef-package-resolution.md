# Session Journal — reeyarn — 2026-07-12 20:54

- XBRL Taxonomy Engine > Schema resolution: Served the un-fetchable W3C schemas (`xml.xsd`, `XMLSchema.xsd`) from openesef's own bundled mirror instead of the network (`b2e2aaa`) — www.w3.org returns 403 to every non-browser client (the code already sent a browser User-Agent, so spoofing was never a fix), which meant all 10,287 filings paid for a doomed HTTP round-trip and then handed lxml a cache path that was never written. `Resolver.bundled_mirror()` is now consulted BEFORE the network, so the doomed request disappears rather than merely failing gracefully. Force-added the two schemas past `.gitignore:216` and added a `MANIFEST.in` rule, otherwise the fallback would be dead code for anyone installing from PyPI (including MDIS, which symlinks the source).

- XBRL Taxonomy Engine > Linkbase parsing: Fixed whole-linkbase aborts caused by a single XML comment (`13f81eb`) — lxml does not give a comment node a string tag (`child.tag` IS the `etree.Comment` function), so `child.tag.lower()` raised `AttributeError: 'cython_function_or_method' object has no attribute 'lower'`, and because linkbase compilation sits inside a broad `except`, that one comment aborted the ENTIRE linkbase. Measured on BIC (100013/2023): calculation base sets 8 → 14, `calc_df` rows 177 → 264 — one `<!-- -->` was silently deleting a third of the filing's calculation network while `concept_df` stayed intact, which is why nobody noticed. Fixed at all four sites (the calc loop that failed plus the three label loops carrying the identical latent bug).

- XBRL Taxonomy Engine > Error reporting: Split "linkbase MISSING" from "linkbase BROKEN" and made the log say whether it matters (`1d91dd6`) — Michelin declares a `linkbaseRef` to a `_ref.xml` it never ships; a reference linkbase carries only citations, so the filing parses complete (450 concepts, 6 statements, SFP/SOP/CFS all found), yet openesef reported it as an ERROR with a full traceback, indistinguishable from a failure that destroys data. A missing `_ref` is now a one-line WARNING; a missing `_pre`/`_cal`/`_def`/`_lab` is an ERROR that says "the taxonomy is incomplete". Motivated directly by the BIC bug: "catch it, log it, carry on" is what let a comment eat a calculation network silently.

- ESEF Integration > Reference resolution: Recovered ESEF packages whose files were RENAMED off the mandated LEI name (`ee5866d`) — ESEF requires files named after the entity's LEI and every internal href obeys that, but JM (102840/2023) references `529900X0UEM9DOM6FK12-2023-12-31_cal.xml` while shipping `jm-2023-12-31_cal.xml`. The reference is correct; the filename is the violation. Because entry points are passed explicitly the filing "parsed" (818 concepts) while losing its ENTIRE calculation network and every extension label, and reported success. `pool._resolve_by_esef_role()` recovers by the file's stem-independent ROLE (`.xsd`/`_pre`/`_cal`/`_def`/`_lab-xx`), only when exactly one file carries that role so it can never bind the wrong file. Recovery alone was insufficient — concepts are keyed by their schema's REAL location — so `taxonomy.get_concept_by_href()` now retries against the schema actually present and `xlink.identify_objects()` falls back to that accessor on a miss. JM: `calc_df` 0 → 144, extension labels 0/20 → 17/20, definitionArc sets 10 → 16.

- ESEF Integration > Reference resolution: Resolved absolute http(s) references against the copy already inside the package (`ee5866d`) — an ESEF package mirrors the filer's own files under the host+path of the URL that references them (exactly what `META-INF/catalog.xml`'s `rewriteURI` declares), but openesef returned any http href unchanged and went to the company's web server for a file it already had. www.kompap.pl 403s; www.apator.eu never answers. Data was not lost for these (the http reference duplicates the entry point) — what goes away is the hang and the error spam.

- ESEF Integration > Robustness: Bounded the HTTP timeout to `(10, 60)` (`ee5866d`) — `requests.get()` had NO timeout at all, so a filer whose web server is slow or gone (www.apator.eu) blocked a worker indefinitely on a schema sitting inside the filing package anyway.

- XBRL Taxonomy Engine > Testing: Established that every one of these fallbacks is strictly additive — frozen-cache A/B on 20 healthy filings: 20/20 byte-identical `concept_df`; cyclic-linkbase gate: 18/18 parse, 0 segfaults. Each fallback fires only where the lookup previously MISSED, so anything that already resolved is untouched. The cache had to be frozen because the live parse job mutates the shared `xbrl_cache` and silently invalidates any before/after comparison.
